export const GameConfig = {
    // A cell width/height in pixels.
    CELL_SIZE: 30,

    // Length of the DFS path (for DFS mode).
    DFS_PATH_SIZE: 300,

    // Width of the maze on a client's screen in pixels.
    RENDERED_MAZE_WIDTH: 800,

    // Height of the maze on a client's screen in pixels.
    RENDERED_MAZE_HEIGHT: 640,

    // # of frames for animation between cells
    MOVEMENT_ANIMATION_FRAMES: 10,

    // animationSpeed value for the pacman AnimatedSprite.
    PACMAN_ANIMATION_SPEED: .05,
}